# CHOPPED. // SYSTEM TERMINAL

**STATUS:** [REDACTED] // VOL.01 DEPLOYMENT ACTIVE
**ORIGIN:** AUSTIN, TX // 02:00 AM PST
**MISSION:** FUNCTION FOR THE FRICTION.

---

## THE MISSION // THE HOOK

High-Performance Ageless Streetwear engineered for the midnight shift. We don’t hide the grey; we wear it with all-black suede. This terminal serves as the digital chassis for the **CHOPPED.** ecosystem—built for the night-shift legends and veterans of the grind.

## THE CHASSIS // TECHNICAL STACK

The system is forged with industrial-grade tools for maximum resilience and zero friction:

*   **ENGINE:** Next.js (App Router)
*   **ARMOR:** Tailwind CSS / Vanilla CSS
*   **HARDWARE:** Radix UI / shadcn/ui
*   **PROTOCOL:** TypeScript // Secure Gear Integration
*   **TERMINAL:** Roboto Mono (Technical) // Inter (Headers)

## SYSTEM DEPLOYMENT // INITIALIZATION

To initialize the local environment and join the advisory board:

```bash
# Clone the protocol
git clone https://github.com/chopped-unc/chopped-web.git

# Install the hardware
npm install

# Deploy the local node
npm run dev
```

The system will be accessible at `http://localhost:3000`.

## CORE PROTOCOLS // UI STANDARDS

As defined in the **@[BRANDBIBLE.md]**, all UI elements must adhere to the following technical specs:

| THE CODE | THE REALITY | THE CHOP |
| :--- | :--- | :--- |
| **ATMOSPHERE** | Background: `#080808` (Deep Asphalt) | Subtle film grain CSS overlay. |
| **STANCE** | Mobile-First / Touch-First | 44px-48px targets for veterans. |
| **LOCKDOWN** | Industrial borders | 1px border-border logic. |
| **REDACTION** | `[REDACTED]` logic | revealed only on hover/touch. |

## ADDING HARDWARE // COMPONENTS

To add new components to the chassis, use the following protocol:

```bash
npx shadcn@latest add [component-name]
```

This will place the components in the `components/ui` directory for manual optimization.

---

**STILL UP.**
© 2026 CHOPPED. INDUSTRIAL WEAR. AUSTIN, TX.
[REDACTED] // ACCESS CODE REQUIRED.
